# this file contains global properties (host, ssh login) needed for the sync manager app

[config]
conf_dir={{ config_files_path }}
var_dir={{ var_dir_path }}
[ssh]
ssh_host=myserver
ssh_user=myusername